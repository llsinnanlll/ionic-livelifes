import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lifelives.app',
  appName: 'LiveLifes',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
